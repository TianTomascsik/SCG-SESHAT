//! Result reporting: CSV writers (F-17) and the self-contained result
//! directory tree plus suite summary (F-18, F-20). CSV is the only output
//! format SESHAT emits.

pub mod csv;
pub mod results;
